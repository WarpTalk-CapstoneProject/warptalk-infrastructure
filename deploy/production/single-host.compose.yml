name: warptalk-single-host

# Combine with data.compose.yml, infra.compose.yml and app.compose.yml. Set all
# three private IP variables to the same interface in the single-host inventory.
services: {}
