name: warptalk-single-host

# Combine with data.compose.yml and app.compose.yml. Both planes continue to use
# DATA_PRIVATE_IP, so moving to split-host changes only the inventory values.
services: {}
